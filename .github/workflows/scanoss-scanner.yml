name: Scanoss - scanner.c

on:
  push:
    paths:
      - 'scanner.c/**'
  pull_request:
    paths:
      - 'src/**'


jobs:
  call-scanoss-workflow:
    uses: .github/workflows/scanoss.yml
    with:
      FILTERED_PATH: 'scanner.c'